<Window x:Class="JustDub.DownloadProgress"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JustDub"
        Title="Готовим вашу загрузку..."
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        mc:Ignorable="d"
        MaxHeight="100" MaxWidth="500" Height="120" Width="500">
    <StackPanel Margin="10">
        <ProgressBar x:Name="progressBar" Maximum="100" Value="50" Height="30"/>
        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
            <TextBlock x:Name="val" Text="50,0" />
            <TextBlock Text="/"/>
            <TextBlock x:Name="max" Text="100,0"/>
        </StackPanel>
    </StackPanel>
</Window>
